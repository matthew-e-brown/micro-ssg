On index page

inserting partial:
    {{> head pageTitle="Index Page" }}

inserting data:
    key = {{ key }}
    hello = {{ hello }}

this text should have two hyphens before it:
{{ prepend hello }}

this should have exclamation marks around it:
{{#surround}}why are we yelling{{/surround}}
