Page: index

Inserting partial:
{{> head pageTitle="Index Page" }}

Inserting data:
key = {{ key }}
hello = {{ hello }}
_shared.yeet = {{ _shared.yeet }}

Prepending hyphens:
{{ prepend hello }}

Surrounding with exclamation points:
{{#surround}}why are we yelling{{/surround}}
